== Welcome to Caball on Guthub

Hello, Bro's and Brajette's. 

  class CaballController < ActionController::Base
    def destroy
      @weblog = Weblog.find(params[:id])
      @weblog.destroy
      logger.info("#{Time.now} Destroyed Weblog ID ##{@weblog.id}!")
    end
  end

The result will be a message in your log file along the lines of:

  Mon Nov 08 14:22:29 +1000 2012 Destroyed Weblog ID #1!

More information on http://www.caball.net/
