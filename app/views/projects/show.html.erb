<section id="promo" class="section alt">
  <%= render 'promo-alt' %>
</section>

<section id="about" class="section">
  <%= render 'about' %>
</section>

<% if @project.roles.present? then %>
<section id="roles" class="section alt">
  <%= render 'roles' %>
</section>
<% if @project.comments.present? %>
<section id="roles" class="section alt">
<div id="comments">
	<div class="container-fluid">
	    <div class="row-fluid">
	      <div class="span12">
	        <div class="row-fluid">
	          <div class="page-header">
	              <h1>
	                  Project Updates
	              </h1>
	          </div>
	        </div>
  				<%= render @project.comments %>
				</div>
		  </div>
	</div>
</div>
<% end %>
<% if current_user %>
<% if current_user.id == @project.user_id %>
<div id="comments">
	<div class="container-fluid">
	    <div class="row-fluid">
	      <div class="span12">
	        <div class="row-fluid">
	          <div class="page-header">
	              <h1>
	                  Create a New Update
	              </h1>
	          </div>
	        </div>
  				<%= render 'comments/form' %>
				</div>
		  </div>
	</div>
</div>
<% end %>
<% end %>


</section>

<% end %>