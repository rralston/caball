<section class='projects-search'>
  <div class='container'>
    <div class='row'>
      <!-- Left Column for Search Terms-->
      <div class='span3 search-column'>
        <div class="projects-search-title">
          <h3>PROJECTS</h3>
        </div>
        <div class="projects-search-content">
          <%= search_form_for @search do |f| %>
          <div class="field project-title">
            <%= f.text_field :title_cont, :placeholder => 'Find a Project...', :class => 'project-title-input' %>
            <i class="icon-search"></i>
          </div>
          
          <div class="field genre">
            <h3>Genre</h3>
            <%= f.select(:genre_cont, options_for_select(@genres), {}, {:multiple=> true, :size => 5, :class => 'genres-input' }) 
            %>
          </div>
          <div class="field location">
            <h3>Location</h3>
            <%= f.text_field :location_cont, :placeholder => 'Location' , :class => 'location-input' 
            %>
          </div>
          <div class="field roles">
            <h3>Open Roles</h3>
            <%= f.select(:roles_id_cont, options_for_select(@talents), {}, {:multiple=> true, :size => 5, :class => 'roles-input' })
            %>
          </div>
          <% end %>
        </div>
      </div>
      
      <!-- Right Column for displaying users-->
      <div class='span9 search-results'>
        <%= render :partial => 'project_search_results' %>
      </div>

  </div>
</section>

<%
=begin
%>
<div class="well">
<section class='section' id='content' role='main'>
  <div class='container-fluid'>
    <div class='page-header'>
	
			<!-- Begin New Project Search -->

			<div class="accordion" id="accordion2">
			  <div class="accordion-group">
			    <div class="accordion-heading">
			      <a class="accordion-toggle" data-toggle="collapse" data-parent="#accordion2" href="#collapseOne">
			        <h1>Advanced Search Fields</h1>
			      </a>
			    </div>
			    <div id="collapseOne" class="accordion-body collapse out">
			      <div class="accordion-inner">
			        <%= search_form_for @search do |f| %>
							  <%= f.condition_fields do |c| %>
							  <div class="field">
							    <%= c.attribute_fields do |a| %>
							      <%= a.attribute_select associations: [:roles] %>
							    <% end %>
							    <%= c.predicate_select %>
							    <%= c.value_fields do |v| %>
							      <%= v.text_field :value %>
							    <% end %>
							    </div>
							  <% end %>
							  <div class="actions"><%= f.submit "Search" %></div>
							<% end %>
			      </div>
			    </div>
			  </div>
				<!-- End New Project Search -->
		  </div>
    </div>
				
      <h1>
        Featured Projects
        <small>Here are just some of the great projects on Caball right now.</small>
      </h1>
    <div class="row-fluid">
      <% @projects.each_with_index do |proj, counter| %>
			<div class='span4 projects-box'>
        <%= link_to project_path(proj.id) do %>
          <div class='thumbnail-figure'>
            <% if proj.photos.exists? %>
						  <%= image_tag(proj.photos.first.image.url(:large)) %> </img>
						<% end %>
					</div>
				<% end %>
        <div class='thumbnail-title'>
          <h3>
            <span><%= proj.title %></span>
          </h3>
          <p>
							<%= proj.roles.length %> Roles Available!
          </p>
        </div>
        <p>
          <%= shorten :text => proj.description, :length => 500, :partial => "more_projects_description", :counter => counter %>
        </p>
        </div>
      <% end %>
    </div>
  </div>
</section>
<br>
<%- model_class = Project.new.class -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
	    <th><%= model_class.human_attribute_name(:id) %></th>
			<th><%= model_class.human_attribute_name(:user_id) %></th>
			<th>User Photo</th>
      <th><%= model_class.human_attribute_name(:title) %></th>
      <th><%= model_class.human_attribute_name(:description) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%= model_class.human_attribute_name(:end) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @projects.each_with_index do |project, counter| %>
      <tr>
        <td><%= link_to project.id, project_path(project), :class => 'btn btn-mini' %></td>
				<td><% if project.user_id %><%= link_to User.find(project.user_id).name, User.find(project.user_id) %><% end %></td>
				<td><% if User.find(project.user_id).photos.exists? %><%= image_tag(User.find(project.user_id).photos.first.image.url(:medium), {:class => "img-polaroid"}) %><% end %></td>
				<td><%= link_to project.title, project_path(project) %></td>
        <td>
          <%= shorten :text => project.description, :length => 100, :partial => "more_projects_description", :counter => counter %>
          <!-- Projects Description Modal -->

          <div id="projects-description-<%= counter %>" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
            <div class="modal-header">
              <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
              <h3><span><%= project.title %></span></h3>
            </div>
            <div class="modal-body">
              <p>
                <%= simple_format(project.description) %>
              </p>
            </div>
          </div>
          <!-- End Projects Description Modal -->
        </td>
        <td><%=l project.created_at %></td>
				<td><%= project.end %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_project_path(project), :class => 'btn btn-warning btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      project_path(project),
                      :method => :delete,
                      :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')),
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<!-- <%= link_to t('.new', :default => t("helpers.links.new")),
            new_project_path,
            :class => 'btn btn-primary' %> -->
<br>
<%
=end
%>
</div>
</div>
