<script type="script/template" id='comments_template'>
  
  <div class='row add-comment'>
    <div class='span8'>
      <div class='row'>
        <% if current_user %>
          <img src='<%= current_user.profile_pic %>' class='img-circle user-profile-img'>
        <% end %>

        <%= form_for Comment.new, :remote => true do |c|  %>
          <%= c.hidden_field :commentable_type, :value => 'Project' %>
          <%= c.hidden_field :commentable_id, :value => @project.id %>
          <%= c.text_area :content, :placeholder => 'Add your project updates here'  %>
          <%= c.fields_for :photo, Photo.new do |p| %>
            <%= p.file_field :image %>
          <% end %>
        <% end %>
      </div>
      <div class='row'>
        <% if not current_user %>
          <div class='span8' id='please_sign_in' style='disaplay:none;'>
            <a href='/'>Sign in</a> to start discussing.
          </div>  
        <% end %>
        <div class='span8' id='comment_please_wait' style='disaplay:none;'>
          Please wait while we add your comment..
        </div>
      </div>
    </div>
    
    

  </div>

  <div id='comments_display'>
    
  </div>
</script>